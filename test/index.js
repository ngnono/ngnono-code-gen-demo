import assert from 'assert';
import ngnonoCodeGen from '../lib';

describe('ngnono-code-gen', function () {
  it('should have unit test!', function () {
    assert(false, 'we expected this package author to add actual unit tests.');
  });
});
